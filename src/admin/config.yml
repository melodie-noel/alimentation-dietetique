# --- AUTHENTIFICATION ---
# Netlify Identity / Git Gateway étant dépréciés, on passe par DecapBridge.
# À COMPLÉTER après création du compte sur https://decapbridge.com :
#   - repo : votre-utilisateur/melodie-noel
#   - identity_url / gateway_url : fournis par DecapBridge
# Pendant les tests, on cible la branche preprod.
backend:
  name: git-gateway
  branch: preprod
  # repo: VOTRE-UTILISATEUR/melodie-noel
  # identity_url: https://auth.decapbridge.com/sites/VOTRE_SITE_ID
  # gateway_url: https://gateway.decapbridge.com

locale: fr
media_folder: "src/assets/img"
public_folder: "/assets/img"

collections:
  - name: pages
    label: Pages du site
    files:
      - name: home
        label: Accueil
        file: src/_data/home.json
        format: json
        fields:
          - label: "Hero (haut de page)"
            name: hero
            widget: object
            fields:
              - { label: "Surtitre", name: eyebrow, widget: string }
              - { label: "Titre", name: title, widget: string, hint: "Vous pouvez garder <br> et <span class=\"hand\">…</span> pour le style manuscrit." }
              - { label: "Sous-titre", name: subtitle, widget: text, hint: "Conservez les <span class=\"kw\">…</span> pour les mots en couleur." }
              - { label: "Libellé du bouton", name: ctaLabel, widget: string }
          - label: "Section « Mon approche »"
            name: approche
            widget: object
            fields:
              - { label: "Surtitre", name: eyebrow, widget: string }
              - { label: "Titre", name: title, widget: string }
              - { label: "Introduction", name: intro, widget: text }
              - { label: "Slogan", name: slogan, widget: string }
              - label: "Les deux piliers"
                name: pills
                widget: list
                max: 2
                fields:
                  - { label: "Étiquette", name: tag, widget: string }
                  - { label: "Titre", name: title, widget: string }
                  - { label: "Texte", name: text, widget: text }
              - { label: "Phrase de fin", name: line, widget: string }
          - label: "Section « Pourquoi venir me voir »"
            name: raisons
            widget: object
            fields:
              - { label: "Surtitre", name: eyebrow, widget: string }
              - { label: "Titre", name: title, widget: string }
              - { label: "Introduction", name: intro, widget: text }
              - label: "Motifs de consultation (icône fixe)"
                name: items
                widget: list
                max: 6
                fields:
                  - { label: "Titre", name: title, widget: string }
                  - { label: "Texte", name: text, widget: text }
          - label: "Section « En entreprise »"
            name: entreprise
            widget: object
            fields:
              - { label: "Surtitre", name: eyebrow, widget: string }
              - { label: "Titre", name: title, widget: string }
              - { label: "Phrase", name: intro, widget: text }
              - { label: "Points", name: points, widget: list, field: { label: "Point", name: point, widget: string } }
              - { label: "Libellé du bouton", name: ctaLabel, widget: string }
          - label: "Bloc « Premier rendez-vous »"
            name: cta
            widget: object
            fields:
              - { label: "Surtitre", name: eyebrow, widget: string }
              - { label: "Titre", name: title, widget: string }
              - { label: "Texte", name: text, widget: text }
              - { label: "Libellé du bouton", name: ctaLabel, widget: string }
              - { label: "Infos pratiques", name: meta, widget: string }
